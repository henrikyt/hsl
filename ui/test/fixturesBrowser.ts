/**import { setupWorker } from "msw/browser";
import { getDefaultMock } from "../src/api/gen/default/default.msw";

const handlers = [...getDefaultMock()];

export const worker = setupWorker(...handlers);
*/
